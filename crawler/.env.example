# Local PostgreSQL connection fields. Keep the real password in crawler/.env
# or a deployment secret manager, never in Git.
host=127.0.0.1
port=5432
username=postgres
password=replace-me
database=gogetsomefoodferris

# The daemon reloads active Job Links after every cycle.
CRAWL_INTERVAL_SECONDS=3600
